import 'package:flutter/material.dart';

class StaffDetailsScreeen extends StatelessWidget {
  const StaffDetailsScreeen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
